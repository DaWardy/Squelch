# ── Critical — Squelch will not start without these ────────────────────────
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip>=13.6.0,<14
requests>=2.31.0
python-dateutil>=2.8.2
defusedxml>=0.7.1
appdirs>=1.4.4

# ── Strongly recommended — most features need these ────────────────────────
pyserial>=3.5
maidenhead>=1.0.0
adif-io>=0.0.5
keyring>=24.0.0
psutil>=5.9.0

# ── Optional — enables specific features ───────────────────────────────────
# SDR waterfall and spectrum display
pyqtgraph>=0.13.3
numpy>=1.26.0

# Audio features
sounddevice>=0.4.6
soundfile>=0.12.1

# Map features — the interactive Leaflet map. WITHOUT these you get the crude
# Qt fallback renderer (no satellites, no propagation path overlays, etc.).
# Versions MUST match PyQt6 (6.6.x here); a mismatch causes a DLL load error.
PyQt6-WebEngine==6.6.0
PyQt6-WebEngine-Qt6==6.6.1
folium>=0.15.0

# Digital mode features
aiohttp>=3.9.0
websockets>=12.0

# Location and grid features
geopy>=2.4.0
mgrs>=1.4.4

# Logging and export
xmltodict>=0.13.0
Markdown>=3.5.0
Pillow>=10.0.0

# Ham radio data
pyhamtools>=0.6.8

# Scipy (signal processing) - may fail on Python 3.14
scipy>=1.11.0

# Windows COM port access
pywin32>=306; sys_platform == "win32"

# Windows Location API (one-shot OS position for the station grid).
# Optional — GPS-serial and manual entry work without it.
winsdk>=1.0.0b10; sys_platform == "win32"

# ── Development / Testing ───────────────────────────────────────────────────

sgp4>=2.22      # satellite orbit prediction
